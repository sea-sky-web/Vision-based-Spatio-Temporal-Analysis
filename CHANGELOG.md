# Changelog

## [Unreleased]
### Added
- Documented the MVDet-inspired training overhaul, including the CenterNet-style BEV detection head with focal heatmap, offset, and size regression losses.
- Noted the dataset target enrichment (default meter footprints) and augmented preprocessing pipeline with color jitter plus ImageNet normalization.
- Recorded configuration updates exposing loss weights and Gaussian target parameters to support the new supervision scheme.

