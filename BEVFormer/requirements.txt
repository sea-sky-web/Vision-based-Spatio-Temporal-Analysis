# 核心依赖 (torch, mmcv, mmdet, mmdet3d) 请参照主 README.md 中的指导进行安装。
# 本文件仅包含剩余的辅助依赖项。
# 为确保环境的可复现性，我们已将版本固定。

numpy==1.21.6
matplotlib==3.5.3
opencv-python==4.6.0.66
pillow==9.2.0
tqdm==4.64.0
numba==0.55.2
# mmcv-full, mmdet, etc. should be installed via specific commands from README.
# The following are here for reference, but direct installation is recommended.
# mmcv-full==1.3.17
# mmdet==2.14.0
# mmdet3d==0.14.0